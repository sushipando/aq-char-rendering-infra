split up multi-asset prepares, bounds, renders into their own separate small units. ex. render each part of the armor separately. same goes with anything else with multiple parts (helms etc). this reduces the lower bound time for renders as armors are usually the slowest to prepare (outside of giant assets)
create new combine/finalize that uses avif instead. this should be a togglable choice given by the user when they submit a job. also be sure to update aq-image-search's discord_bot.py to have the command to toggle between the two. there should also be a toggle for the lossy amount and we already have a lossless toggle we can reuse. there are some optimizations in other docs here that say how to optimize the avif speed (combine saves raw RGBA or something)
have some sort of db store how many frames an item has that renders over a threshold. that way in the future, when we get to the render step, we can see if all the items have enough frames which suprass the threshold for 40 concurrency, then we should switch to distributed map. the threhsold should be such that a distributed map will be faster by 5 seconds. distributed maps generally add 5 seconds to the longest render time of an individual frame. this is to avoid when we would render a large asset twice in the same lambda, causing major slowness. distriuted map concurrency should be greater or equal to max frames then

# AQW Character Renderer

Python rendering and Lambda/container code will live in this workspace
package. Rendering behavior is migrated from the existing local FFDec pipeline
before distributed handlers are added.
